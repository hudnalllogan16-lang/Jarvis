# Milestone Report — M6: the Affiliate vertical slice

Date: 2026-07-26 · Gates at closure: exit 0, 594 tests · Verdicts: architecture **MERGE**,
product **SHIP WITH FOLLOW-UPS**

## Roadmap delta
M6 (rev. 4's reframing: prove the platform end to end with one company, before widening) is
complete. M7 Finance Tracking is next and unchanged. One resequencing occurred inside M6:
fix packets M6-0b…M6-0h, M6-1b…M6-1d, M6-2b, M6-4a/4b, M6-5a were interposed as execution
surfaced defects — operational changes, not architectural; each is in `docs/packets/` with its
report distilled into DECISIONS.md.

## Dependency-graph delta
Deferred-completion ledger fully closed (last row: `CredentialManager`, retired by M6-3).
`docs/DEPENDENCIES.md` updated (M6-5a round). No new packages; no layering exceptions added.
One flagged duplication: `/api/health` checks exist in both shell (M5) and api (M3) because
sharing them would invert milestones (M6-F44, accepted, future unification).

## Objective and outcome
Objective: one company travels create → wake → act → ask → approve → execute → record, live.
Outcome: **achieved and verified live**, not simulated — on company "Summit Trail Gear":
model-planned three-wave dependency chain (research → content → compliance, D-023), a
`affiliate.publish_post` proposal composed from the compliance-reviewed draft, §8 approval
rendered from stored values, operator approval via the real API, webhook execution with the
credential provably confined to the call (asserted against every log and row), idempotent
replay (one POST total), full audit + Decision Log narrative visible in the operator surface.

## Implementation decisions
D-020…D-025 added (health-band caps; cycle boundary/`cycle_id`; committed budget reservations
serialized per scope; dependency waves; approved-action effect binding; independent denial
commits + Postgres test lane). Each records what it fills, what was rejected, and reversal
cost. No spec conflict found by audit; the spec was never edited.

## Defects found and corrected
44 numbered findings (M6-F1…M6-F44) plus 6 product follow-ups. Of the 44: 33 fixed with
regression tests, 11 open by explicit decision (list in DECISIONS.md "M6 closure"). Highlights
of what only execution could find: authorization read registration-time lifecycle state
(M6-F1); Temporal's converter couldn't encode any payload (M6-F5); the approve endpoint
published no event — D-006's loop open at the human boundary (M6-F20); nothing ever called
`execute_approved_action` (M6-F31); concurrent reservations breached ceilings, verified live
(M6-F12); audited denials were silently rolled back (M6-F38); the dashboard rendered blank
(M6-5 №1). The pre-M6 codebase was written without an interpreter; this is the disclosed debt
being paid, and the architecture survived it — no invariant needed weakening.

## Verification performed
First-ever full execution of everything: suite (394 → 594 tests, all green), ruff/format,
pyright (0 errors), migrations against real Postgres (0001→0006), live Temporal worker + live
model cycles (~$9 total spend incl. reviews), the D-022 race proven live with negative
controls, §10 containment asserted byte-level, both reviewer verdicts on the real running
system. Verified-vs-written discipline held: every packet report separates the two; the audit
independently re-verified claims against the live DB and the pristine pre-M6 tree.

## Known limitations
Open ledger (non-blocking for dev posture, recorded): F13 (context-load failure policy), F16/
F26 (no real pricing; settlement rules diverge), F17 (retry re-mints cycle id), F18 (orphaned
reservations need reconciliation), F25 (no per-company ceiling edit), F33 (**no workflow
versioning — must close before production**), F34 (executed approvals re-planned; paid
reasoning against done work), F42 (credential refusals unaudited), F44 (health-check
duplication), D-025.2 (Postgres test lane), product F1–F6 + runtime-§12.5 term coverage.
The repo has no git; run manifests assume one (owner decision pending).

## Merge recommendation
Merge. Both gating reviews concur; open items are recorded, scoped, and owned.

## Surfaced in the Shell
The operator can see everything M6 built: companies paint with real health/spend/activity
(blank-dashboard defect fixed and re-verified), approvals show the §8 facts *and the actual
payload* with an edit-before-approve path, the Decision Log renders operator-language
narratives with ids stripped and drill-down intact, notifications resolve/dismiss, health is
honest about a company that ships nothing, and `/api/health` answers under both topologies.
