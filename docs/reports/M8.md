# Milestone Report — M8: Plugin Framework + Product Identity

Date: 2026-07-27 · Gates at closure: exit 0, 975 tests (734 at m7-baseline) · Verdicts:
architecture **MERGE**, product **SHIP WITH FOLLOW-UPS** · Live spend: $0.12 (M8 proper: $0)

## Objective and outcome
The owner's dual mandate — evolve the platform through the plugin framework AND establish
Jarvis's permanent product identity — both landed, live-proven:
- **Framework:** designed first (614-line design doc), then implemented as D-029…D-032 —
  injected catalog, install containment/validation/drift-detection, and the contract-refresh
  mechanism whose Band-C guard the audit verified as four independent layers. Proven by the
  three-company live migration through the real consent path: Summit's negative control held
  structurally, Portfolio Watch's attainment corrected 45 → 91, Trailhead's M6-era wake-loop
  snapshot healed. D-030 proven live: configuration consent never touches the approval queue.
- **Product identity:** the design system as a permanent artifact (twelve documents, token
  architecture fenced by test), the Application Shell (four real workspaces; "a nav item is
  a promise that a destination exists"), vendored OFL fonts, spec v1.5's Manager-persona
  amendment applied with personas reserved-not-faked, and the §12.5 boundary catching live
  model prose at render.

## Also closed this milestone
D-033 workflow versioning (M6-F33 — the pre-production requirement) with three patches in
force, each fixture-proven; D-034 resilience (M6-F13/F17/F18/F42); D-035 pause semantics
(owner-ratified Option B, broadened); D-036 metric-semantics amendments; M7's whole carried
surface backlog.

## Defects and record
Findings M8-F1–F175 allocated; the audit confirmed every deferral honest and the record
complete after one recording commit it demanded (a fair catch — the record fell behind the
code in the final sprint). The merge queue took its first conflict in 22 merges and resolved
it by protocol (lane resolves, Manager never resolves silently). M8-F120 established the
verify-mid-flight-relays-against-git norm.

## The organization, measured
Eleven worktree lanes, eleven merges, one conflict (protocol-resolved), zero post-merge gate
failures; two mid-flight cross-lane relays landed without fix rounds; warm resumption used
throughout reviews. The FABLE-RETRO retrospective (committed) becomes M9's operating model:
rolling dispatch, prep pipelining, warm continuity, 4-lane cap — measured against its
+25–35% prediction.

## Known limitations / M9 inbox
The product follow-ups 1–8 + inert "Not now" + decline persistence (recorded in "M8
closure"); M8-F104 (post-M8-F48); M7-F60 (with M10's capability-result semantics); UI Phases
3–5 continue. **Sole tag-gate: M8-F162** — ~445 orphaned test workflows on the live
`default` Temporal namespace (a worker start would execute them against the live DB and real
key); purge paused by the owner mid-packet; no worker starts until resolved.

## Merge recommendation
Merged progressively; both verdicts concur. Tag `m8-baseline` once M8-F162 is resolved by
the owner's chosen route.
