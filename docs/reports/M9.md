# Milestone Report — M9: The Executive Layer and the Governance Constitution

Date: 2026-07-28 · Gates at closure: exit 0, **1239 tests** (976 at m8-baseline) · Verdicts:
architecture **MERGE** (all pre-tag fixes landed), product **SHIP** · Live spend: ~$0.12
(diagnostics only; the deterministic Executive never calls a model by construction)

## What M9 built
**The deterministic Executive (spec §3), complete and live:** portfolio rollup with every
figure naming its window (D-040), the census instead of a score (D-039 — proven right by
live data twice), spend alerts on both ceilings, the circuit breaker's first-ever caller
with the once-per-halt log discipline, all on a supervised 60-second timer (D-041) behind an
import rule that makes "deterministic" mechanically checkable (D-038).

**The Governance Constitution:** EXECUTIVE-GOVERNANCE.md (rev 2, 2,090 lines) under the
owner's four conditions — platform-wide authority (every executable action: Action → Level →
Approval Rule → Audit Record), the formal policy definition, Decision Lineage as a proof
tree, downward-only inheritance — **with its enforcement shipped in the same milestone**:
the Action Registry (24 actions, cross-constraints at import, L4/L5 reserved), the parameter
register with value pins, provenance heads on every governed artifact, namespace + ratchet +
unregistered-action-refusal guards. The ratification package (spec §15-family amendment
texts, the M9-F130 remediation table) awaits the owner.

**The operator surface:** company workspaces with honest trends (relation stated, magnitude
never faked), the census tile, the halt narrative's path to a human, consent surfaces that
say what they do, one voice for the never-measured, and — after the REVISE round — a
platform that cannot say "Running normally" over a failed round.

**The incident (M9-F118):** three companies failed silently at 06:14; diagnosed to a
transient API 404 amplified by a worker outage, made invisible by a structurally silent
FAILED path. Fixed (D-053: failed rounds notify; the model id is validated at the door) and
documented as the governance model's motivating exhibit — Confidence would have read BLIND,
lineage would have exposed it, the census reproduced it on screen until the REVISE round.

## Engineering retrospective
19 lane merges, 3 conflicts (all lane-resolved per protocol), zero post-merge gate failures.
Rolling dispatch (D-037's pilot): successors dispatched within minutes of inputs merging;
packet prep pipelined; warm continuity used extensively (3–10× cheaper, including the
governance revision and both re-reviews). **Failure modes found and recorded:** a dispatch
step silently dropped when a notification interleaved (the m9-8 lane sat empty until the
owner's nudge — lesson: verify dispatch-after-commit); two agents lost to a session limit,
one to a stall, one to a user-stop — all recovered by the resume-or-fresh protocol; one
reviewer force-killed an unowned process (benign; the report-don't-kill rule is now in every
founding mandate). The M8-F120 verify-first norm fired correctly twice more.

## Readiness decision: **YES — recommend m9-baseline.**
All ten close-out objectives met; no unresolved governance violations; both verdicts concur.

## M10 readiness assessment
**Ready with named preconditions.** Trading Intelligence's tactical judgment lands without
rearchitecture (audit-verified): new llm-fenced package, registry entry at L2-tactical,
admissible by schema. **Before M10 starts:** (1) owner rules on platform-scoped approvals
(§8 draft — strategic L2 is unbuildable without it) and ratifies the governance amendments +
remediation table; (2) the evaluation sub-ceiling ships before the first judgment model
call; (3) the type must be named `trading` (namespace welds prefix to name); (4) refresh's
in-or-out registry status is settled (audit F2 — currently a stated exclusion); (5) Decision
Lineage ships with its first producer (recorded position — no dormant tables); (6) the
consent-button labels and the four deferred copy minors ride M10's first surface packet;
(7) the JS-runner and build-tooling questions get their retrospective. Pay-later list
(recorded): container seam consolidation, preflight unification, in-module action ratchet
residue (M9-F160/F134).

## Closing
M6 proved the engine. M7 proved it generalizes. M8 gave it a product identity. **M9 gave it
a constitution and the discipline to enforce it on itself.** The platform now refuses
unauthorized authority by construction, tells the operator the truth when it fails, and
knows — mechanically — the difference between executing policy and creating it.
