## Packet M9-9: the product REVISE round

**Agent:** operator-surface-engineer  **Model:** sonnet — five specified fixes.
Findings **M9-F190–F199**. Lane: `lane/m9-9`.

Per the "M9 final verdicts" DECISIONS entry, items 1-5 exactly (the census recount touches
jarvis/executive/health.py — counts recorded READINGS not completed cycles; keep D-039;
never_measured must catch a company with cycles but no measurements) + the four minors as
capacity allows (duplicate zero, per-goal repeat, 0.0005 phrasing, the confident empty-list
claim). §12.5 + escaping + both themes + self-check. Live verify :8110 read-only (start
your OWN server; if a port is taken, pick another port and REPORT the conflict — never kill
a process you did not start). $0. Gates exit 0; commit "M9-9: "; never merge/push; no
DECISIONS.md edits. Report 300/450.
