## Packet M9-3: the M8 surface backlog (M9 wave 0, Lane C)

**Agent:** operator-surface-engineer  **Model:** sonnet — nine decided items, recorded in
DECISIONS "M8 closure" (the product review''s 1–8 + stale tokens.css comment). Findings
**M9-F40–F49**. Lane: `lane/m9-3`.

Items verbatim from the record: Approvals-route teaching empty state; newco install-failure
surfacing (.formErr pattern); tile-vs-card severity tone (watch must not escalate to
risk-red — extend the tile tones per design-system rule); healthy-labelled-nothing-achieved
wording (never-measured companies: coordinate semantics with kpi engine ONLY at render —
no engine changes, flag if insufficient); "Working — details inside." copy; up-to-date vs
uncomputable distinguished with one honest line; freshness value/unit copy; in-app theme
toggle in Settings (persisted, honors OS default until set); tokens.css M8-F21 stale comment.
Scope: api/render.py, static/ (NOT the approval card), kpi/engine.py summary WORDING only.
§12.5 + self-check. Live verify :8110; $0. Gates exit 0; commit "M9-3: "; never merge/push.
Report 400/550.
