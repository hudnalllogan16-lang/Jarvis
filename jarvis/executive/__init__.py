"""The Executive Layer's deterministic half (spec §3, D-038…D-041).

Computes and reports over stored values; never writes to a contract (D-038).
See docs/design/EXECUTIVE-LAYER.md Parts 1-3 for the design this implements
and Part 10 for what it deliberately does not do yet.
"""
